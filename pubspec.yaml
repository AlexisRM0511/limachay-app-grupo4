name: limachayapp
description: Proyecto grupo 4 - Limachay.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.17.1 <3.0.0"

dependencies:
  fluttertoast: ^8.0.9
  firebase_core: ^1.19.1
  cloud_firestore: ^3.2.1
  firebase_auth: ^3.4.1
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  charts_flutter: ^0.12.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  http: ^0.13.4
  intl: ^0.17.0
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/img.png
    - assets/loginLogo.png
    - assets/default_sue.png